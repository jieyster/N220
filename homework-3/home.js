let radius = 5;
// The radius might need to change later
const area = Math.PI * radius ** 2;
//the equation needs to remain the same
console.log("area", area);


const perimeter = 2 * Math.PI * radius;
//the equation needs to remain the same

console.log("perimeter", perimeter)

